<h1>Here are all the blog posts: </h1>

<% @posts.each do |post| %>
  <div class="well">
    <h2> <%= post.title %> </h2>
    <p> <%= post.body %> </p>
    <%= link_to "Read more", post_path(post), :class => 'btn btn-default' %>
  </div>
<% end %>
